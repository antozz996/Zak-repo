openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Zak Ecosystem AI - CRM Omnicanale API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Staff authentication and authorization
  - name: utenti
    description: Staff users
  - name: contatti
    description: CRM contacts and leads
  - name: preventivi
    description: Event quotes
  - name: agenda
    description: Personal agenda
  - name: task
    description: Personal tasks separated from calendar events
  - name: chat
    description: Unified chat messages
  - name: dashboard
    description: Dashboard summaries and analytics
  - name: calendar
    description: Calendar availability and Google Calendar synchronization
  - name: webhooks
    description: External webhook receivers
  - name: automazioni
    description: CRM automation jobs and configuration
  - name: audit
    description: Staff action audit log
  - name: production
    description: Production readiness and go-live checks

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ── UTENTI ──────────────────────────────────────────────────────────────────
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Authenticate a staff user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthLoginInput"
      responses:
        "200":
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "401":
          description: Invalid credentials

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: End current client session
      responses:
        "200":
          description: Session closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthLogoutResponse"

  /auth/me:
    get:
      operationId: getCurrentUser
      tags: [auth]
      summary: Get current authenticated staff user
      responses:
        "200":
          description: Authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"
        "401":
          description: Missing or invalid session

  /auth/bootstrap-admin:
    post:
      operationId: bootstrapAdmin
      tags: [auth]
      summary: Create or secure the first admin account
      description: Creates the first admin when no users exist, or sets password for an existing admin when guarded by ZAK_BOOTSTRAP_ADMIN_TOKEN.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BootstrapAdminInput"
      responses:
        "201":
          description: Bootstrap admin ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"
        "403":
          description: Bootstrap not allowed

  /utenti:
    get:
      operationId: listUtenti
      tags: [utenti]
      summary: List all staff users
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Utente"
    post:
      operationId: createUtente
      tags: [utenti]
      summary: Create a staff user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UtenteInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Utente"

  /utenti/{id}:
    get:
      operationId: getUtente
      tags: [utenti]
      summary: Get a staff user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Utente"
    patch:
      operationId: updateUtente
      tags: [utenti]
      summary: Update a staff user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UtenteUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Utente"
    delete:
      operationId: deleteUtente
      tags: [utenti]
      summary: Delete a staff user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /preventivi/{id}/versioni:
    get:
      operationId: listPreventivoVersioni
      tags: [preventivi]
      summary: List quote versions
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PreventivoVersione"
    post:
      operationId: createPreventivoVersione
      tags: [preventivi]
      summary: Create a quote version snapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreventivoVersioneInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreventivoVersione"
        "404":
          description: Quote not found

  /preventivi/{id}/invia-whatsapp:
    post:
      operationId: sendPreventivoWhatsApp
      tags: [preventivi]
      summary: Send quote summary to the contact via WhatsApp
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreventivoWhatsAppSendResult"
        "404":
          description: Quote not found

  /preventivi/{id}/pdf:
    get:
      operationId: downloadPreventivoPdf
      tags: [preventivi]
      summary: Download an event quote PDF
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "404":
          description: Quote not found

  /preventivi/{id}/conferma-digitale:
    post:
      operationId: confirmPreventivoDigitale
      tags: [preventivi]
      summary: Digitally confirm an event quote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreventivoConfermaDigitaleInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreventivoConfermaDigitaleResult"
        "404":
          description: Quote not found
        "409":
          description: Requested event date is already confirmed for another quote

  # ── CONTATTI CRM ─────────────────────────────────────────────────────────────
  /contatti:
    get:
      operationId: listContatti
      tags: [contatti]
      summary: List CRM contacts
      parameters:
        - name: stato_lead
          in: query
          schema:
            type: string
        - name: tipo_evento
          in: query
          schema:
            type: string
        - name: origine_lead
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Contatto"
    post:
      operationId: createContatto
      tags: [contatti]
      summary: Create a CRM contact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContattoInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contatto"
        "409":
          description: Contact already exists with the same normalized phone or Instagram username

  /contatti/{id}:
    get:
      operationId: getContatto
      tags: [contatti]
      summary: Get a CRM contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contatto"
    patch:
      operationId: updateContatto
      tags: [contatti]
      summary: Update a CRM contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContattoUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contatto"
        "409":
          description: Contact already exists with the same normalized phone or Instagram username
    delete:
      operationId: deleteContatto
      tags: [contatti]
      summary: Delete a CRM contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /contatti/import-csv:
    post:
      operationId: importContattiCsv
      tags: [contatti]
      summary: Import CRM contacts from CSV text with phone/social deduplication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportContattiCsvInput"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportContattiCsvResult"

  /contatti/{id}/messaggi:
    get:
      operationId: getContattoMessaggi
      tags: [contatti]
      summary: Get chat messages for a contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Messaggio"

  /contatti/{id}/storico-stato:
    get:
      operationId: getContattoStoricoStato
      tags: [contatti]
      summary: Get lead status history for a contact
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StatoLeadStoricoItem"

  /messaggi/{id}/read:
    patch:
      operationId: markMessaggioRead
      tags: [chat]
      summary: Mark a message as read
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Messaggio"

  # ── PREVENTIVI EVENTI ────────────────────────────────────────────────────────
  /preventivi:
    get:
      operationId: listPreventivi
      tags: [preventivi]
      summary: List event quotes
      parameters:
        - name: stato_evento
          in: query
          schema:
            type: string
        - name: contatto_id
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Preventivo"
    post:
      operationId: createPreventivo
      tags: [preventivi]
      summary: Create an event quote
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreventivoInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Preventivo"
        "409":
          description: Requested event date is already confirmed for another quote

  /preventivi/calcola-prezzo:
    post:
      operationId: calculatePreventivoPricing
      tags: [preventivi]
      summary: Calculate quote pricing from package, guests and extras
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreventivoPricingInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreventivoPricingResult"
        "400":
          description: Invalid pricing request

  /preventivi/{id}:
    get:
      operationId: getPreventivo
      tags: [preventivi]
      summary: Get an event quote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Preventivo"
    patch:
      operationId: updatePreventivo
      tags: [preventivi]
      summary: Update an event quote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreventivoUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Preventivo"
        "409":
          description: Requested event date is already confirmed for another quote
    delete:
      operationId: deletePreventivo
      tags: [preventivi]
      summary: Delete an event quote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # ── AGENDA PERSONALE ─────────────────────────────────────────────────────────
  /agenda:
    get:
      operationId: listAgenda
      tags: [agenda]
      summary: List personal agenda items
      parameters:
        - name: categoria
          in: query
          schema:
            type: string
        - name: data_da
          in: query
          schema:
            type: string
        - name: data_a
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AgendaItem"
    post:
      operationId: createAgendaItem
      tags: [agenda]
      summary: Create an agenda item
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgendaItemInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgendaItem"

  /agenda/import-numbers-csv:
    post:
      operationId: importAgendaNumbersCsv
      tags: [agenda]
      summary: Import agenda events from an Apple Numbers monthly planner CSV
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportAgendaNumbersCsvInput"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportAgendaNumbersCsvResult"

  /agenda/{id}:
    get:
      operationId: getAgendaItem
      tags: [agenda]
      summary: Get an agenda item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgendaItem"
    patch:
      operationId: updateAgendaItem
      tags: [agenda]
      summary: Update an agenda item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgendaItemUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgendaItem"
    delete:
      operationId: deleteAgendaItem
      tags: [agenda]
      summary: Delete an agenda item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # ── CHAT MESSAGES (Unified Inbox) ─────────────────────────────────────────────
  /task-personali:
    get:
      operationId: listTaskPersonali
      tags: [task]
      summary: List personal tasks separated from agenda events
      parameters:
        - name: stato
          in: query
          schema:
            type: string
        - name: priorita
          in: query
          schema:
            type: string
        - name: contatto_id
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TaskPersonale"
    post:
      operationId: createTaskPersonale
      tags: [task]
      summary: Create a personal task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskPersonaleInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskPersonale"

  /task-personali/{id}:
    get:
      operationId: getTaskPersonale
      tags: [task]
      summary: Get a personal task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskPersonale"
    patch:
      operationId: updateTaskPersonale
      tags: [task]
      summary: Update a personal task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskPersonaleUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskPersonale"
    delete:
      operationId: deleteTaskPersonale
      tags: [task]
      summary: Delete a personal task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /b2b/competitor:
    get:
      operationId: listB2BCompetitor
      tags: [b2b]
      summary: List B2B competitor archive entries
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: categoria
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/B2BCompetitor"
    post:
      operationId: createB2BCompetitor
      tags: [b2b]
      summary: Create a B2B competitor archive entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BCompetitorInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BCompetitor"

  /b2b/competitor/{id}:
    get:
      operationId: getB2BCompetitor
      tags: [b2b]
      summary: Get a B2B competitor archive entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BCompetitor"
        "404":
          description: Competitor not found
    patch:
      operationId: updateB2BCompetitor
      tags: [b2b]
      summary: Update a B2B competitor archive entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BCompetitorUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BCompetitor"
        "404":
          description: Competitor not found
    delete:
      operationId: deleteB2BCompetitor
      tags: [b2b]
      summary: Delete a B2B competitor archive entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /b2b/materiali:
    get:
      operationId: listB2BMateriali
      tags: [b2b]
      summary: List B2B competitor materials metadata
      parameters:
        - name: competitor_id
          in: query
          schema:
            type: string
        - name: stato
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/B2BMateriale"
    post:
      operationId: createB2BMateriale
      tags: [b2b]
      summary: Register competitor material metadata
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BMaterialeInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BMateriale"

  /b2b/materiali/{id}:
    patch:
      operationId: updateB2BMateriale
      tags: [b2b]
      summary: Update competitor material metadata
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BMaterialeUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BMateriale"
        "404":
          description: Material not found
    delete:
      operationId: deleteB2BMateriale
      tags: [b2b]
      summary: Delete competitor material metadata
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /b2b/template:
    get:
      operationId: listB2BTemplate
      tags: [b2b]
      summary: List B2B co-branding templates
      parameters:
        - name: target_tipo
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/B2BTemplate"
    post:
      operationId: createB2BTemplate
      tags: [b2b]
      summary: Create a B2B co-branding template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BTemplateInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BTemplate"

  /b2b/template/{id}:
    patch:
      operationId: updateB2BTemplate
      tags: [b2b]
      summary: Update a B2B co-branding template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BTemplateUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BTemplate"
        "404":
          description: Template not found
    delete:
      operationId: deleteB2BTemplate
      tags: [b2b]
      summary: Delete a B2B co-branding template
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /b2b/analisi-competitor:
    post:
      operationId: analyzeB2BCompetitor
      tags: [b2b]
      summary: Generate a structured competitor analysis
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BCompetitorAnalysisInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BCompetitorAnalysisResult"

  /b2b/export:
    post:
      operationId: exportB2BPitch
      tags: [b2b]
      summary: Export a B2B pitch as PDF or presentation outline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/B2BExportInput"
      responses:
        "200":
          description: Export generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/B2BExportResult"

  /messaggi:
    get:
      operationId: listMessaggi
      tags: [chat]
      summary: List all chat messages across channels
      parameters:
        - name: canale
          in: query
          schema:
            type: string
        - name: contatto_id
          in: query
          schema:
            type: string
        - name: letto
          in: query
          schema:
            type: boolean
        - name: stato_lead
          in: query
          schema:
            type: string
        - name: operatore_id
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 300
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Messaggio"
    post:
      operationId: sendMessaggio
      tags: [chat]
      summary: Send a message to a contact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessaggioInput"
      responses:
        "201":
          description: Sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Messaggio"
        "409":
          description: WhatsApp 24-hour conversation window is closed for free-form messages

  /chat/inbox:
    get:
      operationId: getChatInbox
      tags: [chat]
      summary: Get unified inbox - one entry per contact with last message
      parameters:
        - name: canale
          in: query
          schema:
            type: string
        - name: stato_lead
          in: query
          schema:
            type: string
        - name: operatore_id
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InboxEntry"

  /chat/assign:
    post:
      operationId: assignChat
      tags: [chat]
      summary: Assign a chat to an operator
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignChatInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contatto"

  /chat/events:
    get:
      operationId: streamChatEvents
      tags: [chat]
      summary: Stream realtime chat events with Server-Sent Events
      description: Server-Sent Events stream for multi-operator inbox updates, read receipts, chat assignment and presence changes.
      responses:
        "200":
          description: SSE stream opened
          content:
            text/event-stream:
              schema:
                type: string

  /chat/typing:
    get:
      operationId: listChatTyping
      tags: [chat]
      summary: List active typing operators for a chat
      parameters:
        - name: contatto_id
          in: query
          required: true
          schema:
            type: string
        - name: canale
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatTypingStatus"
    post:
      operationId: updateChatTyping
      tags: [chat]
      summary: Update operator typing status for a chat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatTypingInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatTypingStatus"

  # ── DASHBOARD & ANALYTICS ────────────────────────────────────────────────────
  /chat/presence:
    get:
      operationId: listChatPresence
      tags: [chat]
      summary: List online/offline operator presence
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OperatorPresence"

  /chat/presence/heartbeat:
    post:
      operationId: heartbeatChatPresence
      tags: [chat]
      summary: Update operator online heartbeat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorPresenceHeartbeatInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorPresence"

  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get dashboard summary stats
      parameters:
        - name: data_da
          in: query
          schema:
            type: string
        - name: data_a
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /dashboard/lead-pipeline:
    get:
      operationId: getLeadPipeline
      tags: [dashboard]
      summary: Get lead counts by status
      parameters:
        - name: data_da
          in: query
          schema:
            type: string
        - name: data_a
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PipelineCount"

  /dashboard/eventi-mese:
    get:
      operationId: getEventiMese
      tags: [dashboard]
      summary: Get confirmed events by month for the current year
      parameters:
        - name: data_da
          in: query
          schema:
            type: string
        - name: data_a
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EventiMeseCount"

  /dashboard/attivita-recente:
    get:
      operationId: getAttivitaRecente
      tags: [dashboard]
      summary: Get recent activity feed
      parameters:
        - name: data_da
          in: query
          schema:
            type: string
        - name: data_a
          in: query
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AttivitaItem"

  /calendar/check-availability:
    get:
      operationId: checkCalendarAvailability
      tags: [calendar]
      summary: Check if a date is available for events using internal data and Google Calendar when configured
      parameters:
        - name: data
          in: query
          required: true
          schema:
            type: string
        - name: slot
          in: query
          required: false
          schema:
            type: string
            enum: [pranzo, pomeriggio, sera, intera_giornata]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarAvailability"

  /calendar/google/status:
    get:
      operationId: getGoogleCalendarStatus
      tags: [calendar]
      summary: Get Google Calendar integration status
      responses:
        "200":
          description: Google Calendar integration status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarStatus"

  /calendar/google/sync:
    post:
      operationId: syncGoogleCalendar
      tags: [calendar]
      summary: Run Google Calendar synchronization
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GoogleCalendarSyncInput"
      responses:
        "200":
          description: Synchronization result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarSyncResult"

  /production/readiness:
    get:
      operationId: getProductionReadiness
      tags: [production]
      summary: Get production go-live readiness checks
      responses:
        "200":
          description: Production readiness report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductionReadiness"

  # ── WEBHOOKS ─────────────────────────────────────────────────────────────────
  # ── AUTOMAZIONI ─────────────────────────────────────────────────────────────
  /automazioni/log:
    get:
      operationId: listAutomazioniLog
      tags: [automazioni]
      summary: Get automation execution log
      parameters:
        - name: tipo
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AutomazioneLog"

  /automazioni/performance:
    get:
      operationId: getAutomazioniPerformance
      tags: [automazioni]
      summary: Get automation performance metrics from execution logs
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutomazioniPerformance"

  /automazioni/config:
    get:
      operationId: listAutomazioniConfig
      tags: [automazioni]
      summary: Get automation configuration settings, including event-type segmentation
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AutomazioneConfig"

  /automazioni/config/{chiave}:
    patch:
      operationId: updateAutomazioneConfig
      tags: [automazioni]
      summary: Update an automation config value such as timing, toggles, or event-type segments
      parameters:
        - name: chiave
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AutomazioneConfigUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutomazioneConfig"

  /automazioni/trigger:
    post:
      operationId: triggerAutomazione
      tags: [automazioni]
      summary: Manually trigger an automation job using the current segmentation config
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriggerManualInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerManualResponse"

  /audit-log:
    get:
      operationId: listAuditLog
      tags: [audit]
      summary: List staff action audit log entries
      parameters:
        - name: azione
          in: query
          schema:
            type: string
        - name: entita
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 300
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditLogItem"

  /webhook/whatsapp:
    get:
      operationId: webhookWhatsappChallenge
      tags: [webhooks]
      summary: Verify Meta WhatsApp webhook challenge
      parameters:
        - name: hub.mode
          in: query
          required: true
          schema:
            type: string
        - name: hub.verify_token
          in: query
          required: true
          schema:
            type: string
        - name: hub.challenge
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Challenge accepted
          content:
            text/plain:
              schema:
                type: string
        "403":
          description: Invalid verify token
    post:
      operationId: webhookWhatsapp
      tags: [webhooks]
      summary: Receive WhatsApp message from Meta
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebhookPayload"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResponse"

  /webhook/voice-assistant:
    post:
      operationId: webhookVoiceAssistant
      tags: [webhooks]
      summary: Receive generic, Vapi, or Bland voice call transcription webhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoiceWebhookPayload"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResponse"

  /webhook/google-calendar:
    post:
      operationId: webhookGoogleCalendar
      tags: [webhooks]
      summary: Receive Google Calendar push notification and trigger incremental sync
      responses:
        "200":
          description: Notification accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookResponse"
        "401":
          description: Invalid Google channel token

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    # ── UTENTE ────────────────────────────────────────────────────────────────
    AuthUser:
      type: object
      required: [id, nome, ruolo, email, stato]
      properties:
        id:
          type: string
        nome:
          type: string
        ruolo:
          type: string
          enum: [admin, manager, staff]
        email:
          type: string
          format: email
        stato:
          type: string
          enum: [attivo, disattivato]

    AuthLoginInput:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        remember:
          type: boolean
          default: false

    AuthSession:
      type: object
      required: [token, expires_at, utente]
      properties:
        token:
          type: string
        expires_at:
          type: string
        utente:
          $ref: "#/components/schemas/AuthUser"

    AuthLogoutResponse:
      type: object
      required: [success]
      properties:
        success:
          type: boolean

    BootstrapAdminInput:
      type: object
      required: [nome, email, password]
      properties:
        nome:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        bootstrap_token:
          type: string
          description: Required when users already exist and ZAK_BOOTSTRAP_ADMIN_TOKEN is configured.

    Utente:
      type: object
      required: [id, nome, ruolo, email, stato, data_creazione]
      properties:
        id:
          type: string
        nome:
          type: string
        ruolo:
          type: string
          enum: [admin, manager, staff]
        email:
          type: string
          format: email
        stato:
          type: string
          enum: [attivo, disattivato]
          description: Stato operativo account staff. Gli account disattivati non devono poter accedere quando verra implementata l'autenticazione.
        data_creazione:
          type: string

    UtenteInput:
      type: object
      required: [nome, ruolo, email]
      properties:
        nome:
          type: string
        ruolo:
          type: string
          enum: [admin, manager, staff]
        email:
          type: string
          format: email
        stato:
          type: string
          enum: [attivo, disattivato]
          default: attivo
        password:
          type: string
          minLength: 8
          description: Optional initial password. Never returned by the API.

    UtenteUpdate:
      type: object
      properties:
        nome:
          type: string
        ruolo:
          type: string
          enum: [admin, manager, staff]
        email:
          type: string
          format: email
        stato:
          type: string
          enum: [attivo, disattivato]
        password:
          type: string
          minLength: 8
          description: Optional password reset. Never returned by the API.

    # ── CONTATTO ──────────────────────────────────────────────────────────────
    Contatto:
      type: object
      required: [id, nome, telefono, origine_lead, stato_lead, handoff_richiesto, data_creazione]
      properties:
        id:
          type: string
        nome:
          type: string
        telefono:
          type: string
        instagram_username:
          type: ["string", "null"]
        origine_lead:
          type: string
        tipo_evento:
          type: ["string", "null"]
        note_interna:
          type: ["string", "null"]
        stato_lead:
          type: string
        handoff_richiesto:
          type: boolean
        data_creazione:
          type: string
        ultimo_contatto:
          type: ["string", "null"]
        operatore_assegnato_id:
          type: ["string", "null"]
        operatore_assegnato_nome:
          type: ["string", "null"]

    ContattoInput:
      type: object
      required: [nome, telefono, origine_lead, stato_lead]
      properties:
        nome:
          type: string
        telefono:
          type: string
        instagram_username:
          type: string
        origine_lead:
          type: string
        tipo_evento:
          type: string
        note_interna:
          type: string
        stato_lead:
          type: string
        handoff_richiesto:
          type: boolean
        operatore_assegnato_id:
          type: string

    ImportContattiCsvInput:
      type: object
      required: [csv]
      properties:
        csv:
          type: string

    ImportContattiCsvResult:
      type: object
      required: [totale_righe, creati, saltati, errori]
      properties:
        totale_righe:
          type: integer
        creati:
          type: integer
        saltati:
          type: integer
        errori:
          type: array
          items:
            type: object
            required: [riga, motivo]
            properties:
              riga:
                type: integer
              motivo:
                type: string

    ContattoUpdate:
      type: object
      properties:
        nome:
          type: string
        telefono:
          type: string
        instagram_username:
          type: string
        origine_lead:
          type: string
        tipo_evento:
          type: string
        note_interna:
          type: string
        stato_lead:
          type: string
        handoff_richiesto:
          type: boolean
        operatore_assegnato_id:
          type: string

    # ── PREVENTIVO ────────────────────────────────────────────────────────────
    Preventivo:
      type: object
      required: [id, contatto_id, stato_evento, data_creazione]
      properties:
        id:
          type: string
        contatto_id:
          type: string
        contatto_nome:
          type: ["string", "null"]
        data_evento_richiesta:
          type: ["string", "null"]
        numero_invitati:
          type: ["integer", "null"]
        budget_stimato:
          type: ["number", "null"]
        note:
          type: ["string", "null"]
        stato_evento:
          type: string
        data_creazione:
          type: string
        google_calendar_id:
          type: ["string", "null"]
        google_event_id:
          type: ["string", "null"]
        google_sync_status:
          type: ["string", "null"]
          enum: [non_configurato, pending, synced, conflict, error, null]
        google_last_synced_at:
          type: ["string", "null"]

    PreventivoInput:
      type: object
      required: [contatto_id, stato_evento]
      properties:
        contatto_id:
          type: string
        data_evento_richiesta:
          type: string
        numero_invitati:
          type: integer
        budget_stimato:
          type: number
        note:
          type: string
        stato_evento:
          type: string
        google_calendar_id:
          type: string
        google_event_id:
          type: string
        google_sync_status:
          type: string
          enum: [non_configurato, pending, synced, conflict, error]

    PreventivoUpdate:
      type: object
      properties:
        data_evento_richiesta:
          type: string
        numero_invitati:
          type: integer
        budget_stimato:
          type: number
        note:
          type: string
        stato_evento:
          type: string
        google_calendar_id:
          type: string
        google_event_id:
          type: string
        google_sync_status:
          type: string
          enum: [non_configurato, pending, synced, conflict, error]

    # ── AGENDA ────────────────────────────────────────────────────────────────
    PreventivoPricingInput:
      type: object
      required: [pacchetto, numero_invitati]
      properties:
        pacchetto:
          type: string
          enum: [essenziale, standard, premium]
        numero_invitati:
          type: integer
        tipo_evento:
          type: string
        extra:
          type: array
          items:
            type: string
            enum: [open_bar, dj_set, fotografo, allestimento, torta, sicurezza]

    PreventivoPricingVoce:
      type: object
      required: [codice, descrizione, quantita, prezzo_unitario, totale]
      properties:
        codice:
          type: string
        descrizione:
          type: string
        quantita:
          type: number
        prezzo_unitario:
          type: number
        totale:
          type: number

    PreventivoPricingResult:
      type: object
      required: [pacchetto, numero_invitati, voci, totale, totale_formattato]
      properties:
        pacchetto:
          type: string
          enum: [essenziale, standard, premium]
        numero_invitati:
          type: integer
        voci:
          type: array
          items:
            $ref: "#/components/schemas/PreventivoPricingVoce"
        totale:
          type: number
        totale_formattato:
          type: string
        note:
          type: string

    PreventivoVersione:
      type: object
      required: [id, preventivo_id, numero_versione, snapshot, data_creazione]
      properties:
        id:
          type: string
        preventivo_id:
          type: string
        numero_versione:
          type: integer
        snapshot:
          type: object
          additionalProperties: true
        nota:
          type: ["string", "null"]
        data_creazione:
          type: string

    PreventivoVersioneInput:
      type: object
      properties:
        nota:
          type: string

    PreventivoWhatsAppSendResult:
      type: object
      required: [success, status, message]
      properties:
        success:
          type: boolean
        status:
          type: string
          enum: [sent, skipped, failed]
        message:
          type: string
        provider_message_id:
          type: ["string", "null"]

    PreventivoConfermaDigitaleInput:
      type: object
      required: [firmatario_nome, metodo]
      properties:
        firmatario_nome:
          type: string
        firmatario_telefono:
          type: string
        metodo:
          type: string
          enum: [firma_digitale, conferma_whatsapp, conferma_manuale]
        note:
          type: string

    PreventivoConfermaDigitaleResult:
      type: object
      required: [preventivo, message]
      properties:
        preventivo:
          $ref: "#/components/schemas/Preventivo"
        message:
          type: string

    AgendaItem:
      type: object
      required: [id, titolo, data_ora_inizio, data_ora_fine, categoria, promemoria_inviato]
      properties:
        id:
          type: string
        titolo:
          type: string
        descrizione:
          type: ["string", "null"]
        data_ora_inizio:
          type: string
        data_ora_fine:
          type: string
        categoria:
          type: string
        contatto_id:
          type: ["string", "null"]
        contatto_nome:
          type: ["string", "null"]
        promemoria_inviato:
          type: boolean
        google_calendar_id:
          type: ["string", "null"]
        google_event_id:
          type: ["string", "null"]
        google_sync_status:
          type: ["string", "null"]
          enum: [non_configurato, pending, synced, conflict, error, null]
        google_sync_direction:
          type: ["string", "null"]
          enum: [zak, google, bidirectional, null]
        google_last_synced_at:
          type: ["string", "null"]
        google_updated_at:
          type: ["string", "null"]

    AgendaItemInput:
      type: object
      required: [titolo, data_ora_inizio, data_ora_fine, categoria]
      properties:
        titolo:
          type: string
        descrizione:
          type: string
        data_ora_inizio:
          type: string
        data_ora_fine:
          type: string
        categoria:
          type: string
        contatto_id:
          type: string
        google_calendar_id:
          type: string
        google_event_id:
          type: string
        google_sync_status:
          type: string
          enum: [non_configurato, pending, synced, conflict, error]
        google_sync_direction:
          type: string
          enum: [zak, google, bidirectional]

    AgendaItemUpdate:
      type: object
      properties:
        titolo:
          type: string
        descrizione:
          type: string
        data_ora_inizio:
          type: string
        data_ora_fine:
          type: string
        categoria:
          type: string
        contatto_id:
          type: string
        promemoria_inviato:
          type: boolean
        google_calendar_id:
          type: string
        google_event_id:
          type: string
        google_sync_status:
          type: string
          enum: [non_configurato, pending, synced, conflict, error]
        google_sync_direction:
          type: string
          enum: [zak, google, bidirectional]

    ImportAgendaNumbersCsvInput:
      type: object
      required: [csv, year]
      properties:
        csv:
          type: string
        year:
          type: integer
          minimum: 2000
          maximum: 2100
        default_month:
          type: integer
          minimum: 1
          maximum: 12
        categoria:
          type: string
          default: lavoro
        p_slot_label:
          type: string
          default: P
        p_start_time:
          type: string
          default: "13:00"
        p_end_time:
          type: string
          default: "17:00"
        c_slot_label:
          type: string
          default: C
        c_start_time:
          type: string
          default: "20:00"
        c_end_time:
          type: string
          default: "23:59"
        dry_run:
          type: boolean
          default: true

    ImportAgendaNumbersCsvItem:
      type: object
      required: [riga, giorno, mese, mese_label, slot, titolo, data, data_ora_inizio, data_ora_fine, acconto_stato, gia_presente, raw_text]
      properties:
        riga:
          type: integer
        giorno:
          type: integer
        mese:
          type: integer
        mese_label:
          type: string
        slot:
          type: string
        titolo:
          type: string
        data:
          type: string
        data_ora_inizio:
          type: string
        data_ora_fine:
          type: string
        acconto_stato:
          type: string
          enum: [si, no, sconosciuto]
        gia_presente:
          type: boolean
        raw_text:
          type: string

    ImportAgendaNumbersCsvResult:
      type: object
      required: [totale_righe, trovati, creati, saltati, errori, items]
      properties:
        totale_righe:
          type: integer
        trovati:
          type: integer
        creati:
          type: integer
        saltati:
          type: integer
        errori:
          type: array
          items:
            type: object
            required: [riga, motivo]
            properties:
              riga:
                type: integer
              motivo:
                type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/ImportAgendaNumbersCsvItem"

    # ── MESSAGGI (Unified Chat) ───────────────────────────────────────────────
    TaskPersonale:
      type: object
      required: [id, titolo, stato, priorita, fonte, data_creazione]
      properties:
        id:
          type: string
        titolo:
          type: string
        descrizione:
          type: ["string", "null"]
        stato:
          type: string
        priorita:
          type: string
        scadenza:
          type: ["string", "null"]
        contatto_id:
          type: ["string", "null"]
        contatto_nome:
          type: ["string", "null"]
        fonte:
          type: string
        data_creazione:
          type: string
        completato_il:
          type: ["string", "null"]

    TaskPersonaleInput:
      type: object
      required: [titolo]
      properties:
        titolo:
          type: string
        descrizione:
          type: string
        stato:
          type: string
        priorita:
          type: string
        scadenza:
          type: string
        contatto_id:
          type: string
        fonte:
          type: string

    TaskPersonaleUpdate:
      type: object
      properties:
        titolo:
          type: string
        descrizione:
          type: string
        stato:
          type: string
        priorita:
          type: string
        scadenza:
          type: string
        contatto_id:
          type: string
        fonte:
          type: string
        completato_il:
          type: string

    B2BCompetitor:
      type: object
      required: [id, nome, categoria, data_creazione, data_aggiornamento]
      properties:
        id:
          type: string
        nome:
          type: string
        categoria:
          type: string
        citta:
          type: ["string", "null"]
        zona:
          type: ["string", "null"]
        target:
          type: ["string", "null"]
        prezzo_medio:
          type: ["integer", "null"]
        rating:
          type: ["integer", "null"]
        punti_forza:
          type: ["string", "null"]
        punti_deboli:
          type: ["string", "null"]
        sito:
          type: ["string", "null"]
        instagram:
          type: ["string", "null"]
        note:
          type: ["string", "null"]
        data_creazione:
          type: string
        data_aggiornamento:
          type: string

    B2BCompetitorInput:
      type: object
      required: [nome]
      properties:
        nome:
          type: string
        categoria:
          type: string
        citta:
          type: string
        zona:
          type: string
        target:
          type: string
        prezzo_medio:
          type: integer
        rating:
          type: integer
        punti_forza:
          type: string
        punti_deboli:
          type: string
        sito:
          type: string
        instagram:
          type: string
        note:
          type: string

    B2BCompetitorUpdate:
      type: object
      properties:
        nome:
          type: string
        categoria:
          type: string
        citta:
          type: string
        zona:
          type: string
        target:
          type: string
        prezzo_medio:
          type: integer
        rating:
          type: integer
        punti_forza:
          type: string
        punti_deboli:
          type: string
        sito:
          type: string
        instagram:
          type: string
        note:
          type: string

    B2BMateriale:
      type: object
      required: [id, nome_file, tipo_materiale, stato, data_creazione]
      properties:
        id:
          type: string
        competitor_id:
          type: ["string", "null"]
        competitor_nome:
          type: ["string", "null"]
        nome_file:
          type: string
        tipo_materiale:
          type: string
        url:
          type: ["string", "null"]
        stato:
          type: string
        note:
          type: ["string", "null"]
        data_creazione:
          type: string

    B2BMaterialeInput:
      type: object
      required: [nome_file]
      properties:
        competitor_id:
          type: string
        nome_file:
          type: string
        tipo_materiale:
          type: string
        url:
          type: string
        stato:
          type: string
        note:
          type: string

    B2BMaterialeUpdate:
      type: object
      properties:
        competitor_id:
          type: string
        nome_file:
          type: string
        tipo_materiale:
          type: string
        url:
          type: string
        stato:
          type: string
        note:
          type: string

    B2BTemplate:
      type: object
      required: [id, titolo, target_tipo, messaggio, utilizzi, data_creazione, data_aggiornamento]
      properties:
        id:
          type: string
        titolo:
          type: string
        target_tipo:
          type: string
        target_descrizione:
          type: ["string", "null"]
        messaggio:
          type: string
        vantaggi:
          type: ["string", "null"]
        cta:
          type: ["string", "null"]
        utilizzi:
          type: integer
        data_creazione:
          type: string
        data_aggiornamento:
          type: string

    B2BTemplateInput:
      type: object
      required: [titolo, messaggio]
      properties:
        titolo:
          type: string
        target_tipo:
          type: string
        target_descrizione:
          type: string
        messaggio:
          type: string
        vantaggi:
          type: string
        cta:
          type: string
        utilizzi:
          type: integer

    B2BTemplateUpdate:
      type: object
      properties:
        titolo:
          type: string
        target_tipo:
          type: string
        target_descrizione:
          type: string
        messaggio:
          type: string
        vantaggi:
          type: string
        cta:
          type: string
        utilizzi:
          type: integer

    B2BCompetitorAnalysisInput:
      type: object
      required: [prompt]
      properties:
        competitor_id:
          type: string
        prompt:
          type: string
        focus:
          type: string
          enum: [prezzo, proposta, debolezze, generale]

    B2BCompetitorAnalysisResult:
      type: object
      required: [titolo, sintesi, punti_forza, punti_deboli, opportunita, azioni_consigliate]
      properties:
        titolo:
          type: string
        sintesi:
          type: string
        punti_forza:
          type: array
          items:
            type: string
        punti_deboli:
          type: array
          items:
            type: string
        opportunita:
          type: array
          items:
            type: string
        azioni_consigliate:
          type: array
          items:
            type: string
        confidence:
          type: string

    B2BExportInput:
      type: object
      required: [titolo, target, formato]
      properties:
        titolo:
          type: string
        target:
          type: string
        messaggio:
          type: string
        budget:
          type: number
        formato:
          type: string
          enum: [pdf, presentazione]

    B2BExportResult:
      type: object
      required: [formato, titolo, contenuto, slides]
      properties:
        formato:
          type: string
          enum: [pdf, presentazione]
        titolo:
          type: string
        contenuto:
          type: string
        download_filename:
          type: string
        slides:
          type: array
          items:
            type: object
            required: [titolo, contenuto]
            properties:
              titolo:
                type: string
              contenuto:
                type: string

    Messaggio:
      type: object
      required: [id, contatto_id, canale, direzione, testo, timestamp]
      properties:
        id:
          type: string
        contatto_id:
          type: string
        canale:
          type: string
        direzione:
          type: string
        testo:
          type: string
        media_id:
          type: ["string", "null"]
        media_tipo:
          type: ["string", "null"]
        media_mime_type:
          type: ["string", "null"]
        media_sha256:
          type: ["string", "null"]
        media_filename:
          type: ["string", "null"]
        timestamp:
          type: string
        letto:
          type: boolean
        mittente_nome:
          type: ["string", "null"]

    MessaggioInput:
      type: object
      required: [contatto_id, canale, testo]
      properties:
        contatto_id:
          type: string
        canale:
          type: string
        testo:
          type: string

    StatoLeadStoricoItem:
      type: object
      required: [id, contatto_id, stato_successivo, origine, data_cambio]
      properties:
        id:
          type: string
        contatto_id:
          type: string
        stato_precedente:
          type: ["string", "null"]
        stato_successivo:
          type: string
        origine:
          type: string
        nota:
          type: ["string", "null"]
        data_cambio:
          type: string

    OperatorPresence:
      type: object
      required: [utente_id, nome, ruolo, online, ultimo_heartbeat]
      properties:
        utente_id:
          type: string
        nome:
          type: string
        ruolo:
          type: string
        online:
          type: boolean
        ultimo_heartbeat:
          type: string

    OperatorPresenceHeartbeatInput:
      type: object
      required: [utente_id]
      properties:
        utente_id:
          type: string

    ChatTypingStatus:
      type: object
      required: [contatto_id, canale, utente_id, utente_nome, is_typing, updated_at]
      properties:
        contatto_id:
          type: string
        canale:
          type: string
        utente_id:
          type: string
        utente_nome:
          type: string
        is_typing:
          type: boolean
        updated_at:
          type: string
        expires_at:
          type: ["string", "null"]

    ChatTypingInput:
      type: object
      required: [contatto_id, canale, utente_id, is_typing]
      properties:
        contatto_id:
          type: string
        canale:
          type: string
        utente_id:
          type: string
        is_typing:
          type: boolean

    InboxEntry:
      type: object
      required: [contatto_id, contatto_nome, telefono, canale, ultimo_messaggio, timestamp, non_letti, stato_lead, handoff_richiesto]
      properties:
        contatto_id:
          type: string
        contatto_nome:
          type: string
        telefono:
          type: string
        canale:
          type: string
        ultimo_messaggio:
          type: string
        timestamp:
          type: string
        non_letti:
          type: integer
        stato_lead:
          type: string
        handoff_richiesto:
          type: boolean
        operatore_assegnato_id:
          type: ["string", "null"]
        operatore_assegnato_nome:
          type: ["string", "null"]

    AssignChatInput:
      type: object
      required: [contatto_id]
      properties:
        contatto_id:
          type: string
        operatore_id:
          type: ["string", "null"]

    # ── DASHBOARD ─────────────────────────────────────────────────────────────
    DashboardStats:
      type: object
      required: [totale_contatti, nuovi_oggi, preventivi_attivi, eventi_confermati, budget_totale_confermato, messaggi_non_letti, lead_con_preventivo, lead_confermati, conversione_lead_preventivo, conversione_preventivo_confermato, conversione_lead_confermato]
      properties:
        totale_contatti:
          type: integer
        nuovi_oggi:
          type: integer
        preventivi_attivi:
          type: integer
        eventi_confermati:
          type: integer
        budget_totale_confermato:
          type: number
        messaggi_non_letti:
          type: integer
        lead_con_preventivo:
          type: integer
        lead_confermati:
          type: integer
        conversione_lead_preventivo:
          type: number
        conversione_preventivo_confermato:
          type: number
        conversione_lead_confermato:
          type: number

    PipelineCount:
      type: object
      required: [stato, count]
      properties:
        stato:
          type: string
        count:
          type: integer

    EventiMeseCount:
      type: object
      required: [mese, count]
      properties:
        mese:
          type: string
        count:
          type: integer

    AttivitaItem:
      type: object
      required: [id, tipo, descrizione, timestamp]
      properties:
        id:
          type: string
        tipo:
          type: string
        descrizione:
          type: string
        timestamp:
          type: string
        contatto_nome:
          type: ["string", "null"]

    CalendarAvailability:
      type: object
      required: [disponibile, date_alternative, slot_disponibili, provider]
      properties:
        disponibile:
          type: boolean
        provider:
          type: string
          enum: [internal, google, combined]
        motivo:
          type: ["string", "null"]
        slot_richiesto:
          type: ["string", "null"]
          enum: [pranzo, pomeriggio, sera, intera_giornata, null]
        slot_disponibili:
          type: array
          items:
            type: string
            enum: [pranzo, pomeriggio, sera, intera_giornata]
        date_alternative:
          type: array
          items:
            type: string

    GoogleCalendarStatus:
      type: object
      required: [configured, enabled, calendar_id, mode]
      properties:
        configured:
          type: boolean
        enabled:
          type: boolean
        calendar_id:
          type: string
        mode:
          type: string
          enum: [env_refresh_token, non_configurato]
        last_full_sync_at:
          type: ["string", "null"]
        last_incremental_sync_at:
          type: ["string", "null"]
        sync_token_available:
          type: boolean
        last_error:
          type: ["string", "null"]
        required_env:
          type: array
          items:
            type: string

    GoogleCalendarSyncInput:
      type: object
      properties:
        direction:
          type: string
          enum: [zak_to_google, google_to_zak, bidirectional]
          default: bidirectional
        full_sync:
          type: boolean
          default: false
        days_ahead:
          type: integer
          default: 180

    GoogleCalendarSyncResult:
      type: object
      required: [configured, direction, pushed, pulled, skipped, conflicts, errors]
      properties:
        configured:
          type: boolean
        direction:
          type: string
        pushed:
          type: integer
        pulled:
          type: integer
        skipped:
          type: integer
        conflicts:
          type: integer
        errors:
          type: array
          items:
            type: string
        message:
          type: string

    ProductionReadinessCheck:
      type: object
      required: [key, status, message]
      properties:
        key:
          type: string
        status:
          type: string
          enum: [ok, warning, missing]
        message:
          type: string
        required_env:
          type: array
          items:
            type: string
        configured_env:
          type: array
          items:
            type: string
        optional_env:
          type: array
          items:
            type: string
        action:
          type: string

    ProductionReadiness:
      type: object
      required: [ready, checks]
      properties:
        ready:
          type: boolean
        checks:
          type: array
          items:
            $ref: "#/components/schemas/ProductionReadinessCheck"

    # ── WEBHOOKS ──────────────────────────────────────────────────────────────
    WebhookPayload:
      type: object
      required: [object]
      properties:
        object:
          type: string
        entry:
          type: array
          items:
            type: object

    VoiceWebhookPayload:
      type: object
      properties:
        provider:
          type: string
          enum: [generic, vapi, bland]
        trascrizione:
          type: string
        telefono:
          type: ["string", "null"]
          description: If present, the webhook links the call to an existing CRM contact with the same normalized phone
        durata:
          type: ["integer", "null"]
        call_id:
          type: ["string", "null"]
        recording_url:
          type: ["string", "null"]
        summary:
          type: ["string", "null"]
        message:
          type: object
          description: Raw Vapi webhook message payload.
        call:
          type: object
          description: Raw Bland or generic call payload.
        data:
          type: object
          description: Raw provider-specific payload wrapper.
        analysis:
          type: object
          description: Provider intent analysis or extracted variables.

    # ── AUTOMAZIONI ────────────────────────────────────────────────────────────
    AutomazioneLog:
      type: object
      required: [id, tipo, stato, data_esecuzione]
      properties:
        id:
          type: string
        tipo:
          type: string
        contatto_id:
          type: ["string", "null"]
        contatto_nome:
          type: ["string", "null"]
        stato:
          type: string
        messaggio:
          type: ["string", "null"]
        data_esecuzione:
          type: string

    AutomazioneConfig:
      type: object
      required: [chiave, valore, descrizione, aggiornato_il]
      properties:
        chiave:
          type: string
        valore:
          type: string
        descrizione:
          type: ["string", "null"]
        aggiornato_il:
          type: string

    AutomazioneConfigUpdate:
      type: object
      required: [valore]
      properties:
        valore:
          type: string

    AutomazioniPerformance:
      type: object
      required: [totale, eseguiti, saltati, errori, tasso_successo, ultimi_30_giorni, per_tipo]
      properties:
        totale:
          type: integer
        eseguiti:
          type: integer
        saltati:
          type: integer
        errori:
          type: integer
        tasso_successo:
          type: number
        ultimi_30_giorni:
          type: integer
        per_tipo:
          type: array
          items:
            type: object
            required: [tipo, totale, eseguiti, saltati, errori]
            properties:
              tipo:
                type: string
              totale:
                type: integer
              eseguiti:
                type: integer
              saltati:
                type: integer
              errori:
                type: integer

    TriggerManualInput:
      type: object
      required: [tipo]
      properties:
        tipo:
          type: string

    TriggerManualResponse:
      type: object
      required: [eseguiti]
      properties:
        eseguiti:
          type: integer
        dettagli:
          type: array
          items:
            type: string

    AuditLogItem:
      type: object
      required: [id, azione, entita, data_creazione]
      properties:
        id:
          type: string
        utente_id:
          type: ["string", "null"]
        utente_nome:
          type: ["string", "null"]
        azione:
          type: string
        entita:
          type: string
        entita_id:
          type: ["string", "null"]
        dettagli:
          type: ["string", "null"]
        ip_address:
          type: ["string", "null"]
        user_agent:
          type: ["string", "null"]
        data_creazione:
          type: string

    WebhookResponse:
      type: object
      required: [success]
      properties:
        success:
          type: boolean
        message:
          type: ["string", "null"]
